# kong-ai-gateway の Konnect hybrid mTLS 証明書用。チャート 0.1.0 は README の
# 記述と違ってビルトインの自己署名 Issuer を生成しないため、自前で用意して
# certificates.cluster.clusterIssuer から明示参照する。
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: selfsigned
spec:
  selfSigned: {}
