apiVersion: v1
kind: Service
metadata:
  name: pii-sanitizer
  namespace: kong
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pii-sanitizer
  ports:
    - name: http
      port: 8080
      targetPort: http
